<Window x:Class="TicTacToe.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TicTacToe"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        Title="Menu" Height="300" Width="300" Background="AliceBlue" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="SlateGray"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Light"/>
        </Style>
    </Window.Resources>
    <Grid>
        <TextBlock Text="Tic Tac Toe" FontSize="50" HorizontalAlignment="Center" FontStyle="Italic" Foreground="Green"/>
        <Button x:Name="PvP" Click ="PvP_Click" Content="Player vs Player"  HorizontalAlignment="Center" Margin="0,-50,0,0" Width="200" Height="50"/>
        <Button x:Name="PvC" Click="PvC_Click" Content="Player vs Computer" HorizontalAlignment="Center" Margin="0,100,0,0" Width="200" Height="50"/>
    </Grid>
</Window>
